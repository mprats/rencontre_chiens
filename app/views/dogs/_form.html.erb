<%= form_for @dog, :html => { :class => 'form-horizontal', multipart: true } do |f| %>
  <% if @dog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dog.errors.count, "error") %> prohibited this dog from being saved:</h2>

      <ul>
      <% @dog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :Nom %><br>
    <%= f.text_field :name %>
  </div>
  
  <div class="field">
    <%= f.label :Race %><br>
    <%= f.text_field :breed %>
  </div>
  
  <div class="field">
    <%= f.label :Date_de_Naissance %><br>
    <%= f.date_select :birthdate, {order: [:day, :month, :year], prompt: { day: 'Jour', month: 'Mois', year: 'Année' }, start_year: Date.today.year, end_year: Date.today.year - 50} %>
  </div>
  
  <div class="field">
    <%= f.label :Genre %><br>
    <%= f.select :gender, ['Male', 'Femelle'] %>
  </div>
  
  <div class="control-group">
    <%= f.label :Photos, :class => 'control-label' %>
    <div class="controls">
      <%= file_field_tag "images[]", type: :file, multiple: true %>
    </div>
  </div>

    <%= f.submit 'Valider', class: 'btn btn-success navbar-btn' %>
<% end %>
